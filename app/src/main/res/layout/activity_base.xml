<?xml version="1.0" encoding="utf-8"?>
<android.support.v4.widget.DrawerLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/drawer_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fitsSystemWindows="true"
    tools:openDrawer="start">

    <include
        layout="@layout/app_bar_base"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <!--the attributes starting with android are the attributes that get applied to framelayout which is root of NavigationBiew-->
    <!-- these attributes start with app because these are not in framework but in support library -->
    <android.support.design.widget.NavigationView
        android:id="@+id/nav_view"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_gravity="start"
        android:fitsSystemWindows="true"
        android:background="@drawable/navigation_bg_drawable"
        app:headerLayout="@layout/nav_header_base"
        app:itemTextAppearance="@style/NavigationTextMenuStyle"
        app:menu="@menu/activity_base_drawer"/>

</android.support.v4.widget.DrawerLayout>
